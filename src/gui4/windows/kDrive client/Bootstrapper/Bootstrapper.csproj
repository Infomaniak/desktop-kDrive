<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
	<Target Name="PreBuild" BeforeTargets="PreBuildEvent">
		<Exec Command="powershell -ExecutionPolicy Bypass -NoProfile -File &quot;$(ProjectDir)..\fetchVersion.ps1&quot; -CsProjPath &quot;$(ProjectDir)..\kDrive client\kDrive client.csproj&quot; -RepositoryRootPath &quot;$(ProjectDir)..\..\..\..\..\&quot;" />
	</Target>
	<Target Name="CleanOutputAfterBuild" AfterTargets="Build">
		<RemoveDir Directories="$(OutputPath)" />
	</Target>
</Project>

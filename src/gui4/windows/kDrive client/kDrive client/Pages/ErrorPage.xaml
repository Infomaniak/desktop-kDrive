<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="KDrive.ErrorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KDrive"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:KDrive.CustomControls"
    xmlns:converters="using:KDrive.Converters"
    xmlns:viewmodels="using:KDrive.ViewModels"
    mc:Ignorable="d">
    <Page.Resources>
    </Page.Resources>

    <Grid Margin="24" RowSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <!-- Title -->
            <RowDefinition Height="Auto"/>
            <!-- BreadCrumb bar -->
            <RowDefinition Height="Auto"/>
            <!-- AppErrors -->
            <RowDefinition Height="Auto"/>
            <!-- SyncErrors -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock x:Uid="ErrorPage_Title" Style="{StaticResource HeaderTextBlockStyle}" Grid.Row="0"/>

        <!-- BreadCrumb bar -->
        <BreadcrumbBar x:Name="NavBar" Grid.Row="1" ItemClicked="NavBar_ItemClicked"/>
        
    </Grid>
</Page>

<Page x:Class="Infomaniak.kDrive.Pages.Onboarding.FinishPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:Infomaniak.kDrive.CustomControls"
      xmlns:converters="using:Infomaniak.kDrive.Converters"
      xmlns:viewmodels="using:Infomaniak.kDrive.ViewModels"
      xmlns:kDrive="using:Infomaniak.kDrive"
      mc:Ignorable="d">
    <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="{StaticResource Infomaniak.Style.Spacing.XXL}">
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="{StaticResource Infomaniak.Style.Spacing.S}">
            <!-- Titre -->
            <TextBlock Text="{x:Bind kDrive:Localizer.Instance.GetString('onboardingSynchronizationAppReadyTitle')}"
                       Style="{StaticResource Infomaniak.Style.TextBlock.Title}"
                       Foreground="{ThemeResource TextFillColorPrimary}" />

            <!-- Sous-titre -->
            <TextBlock Text="{x:Bind kDrive:Localizer.Instance.GetString('onboardingSynchronizationAppReadyDescription')}"
                       Style="{StaticResource Infomaniak.Style.TextBlock.Body}"
                       MaxWidth="350"
                       TextWrapping="WrapWholeWords"
                       Foreground="{ThemeResource TextFillColorSecondary}" />
        </StackPanel>
        <!-- Boutons -->
        <Button Content="{x:Bind kDrive:Localizer.Instance.GetString('buttonOpenKDrive')}"
                Style="{StaticResource Infomaniak.Style.Button.WithBackground}"
                Click="FinishButton_Click" />
    </StackPanel>
</Page>

<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="Infomaniak.kDrive.Pages.Onboarding.DriveSelectionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Infomaniak.kDrive.Pages.Onboarding"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:Infomaniak.kDrive.CustomControls"
      xmlns:converters="using:Infomaniak.kDrive.Converters"
      xmlns:viewmodels="using:Infomaniak.kDrive.ViewModels"
      xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
      xmlns:types="using:Infomaniak.kDrive.Types"
      mc:Ignorable="d">

    <Page.Resources>
        <!-- Converters -->
        <converters:BooleanToVisibilityConverter  x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanToInvertedBooleanConverter  x:Key="BooleanToInvertedBooleanConverter" />
        <converters:IntegerToBooleanConverter  x:Key="IntegerToBooleanConverter" />
        <converters:ColorToBrushConverter x:Key="ColorToBrushConverter" />

        <!-- Data templates -->
        <DataTemplate x:Key="SingleAccountDriveTemplate"
                      x:DataType="types:IDrive">
            <StackPanel Orientation="Horizontal"
                        Spacing="{StaticResource Infomaniak.Style.Spacing.XS}">
                <CheckBox Checked="DriveListCheckBox_Checked"
                          Unchecked="DriveListCheckBox_Unchecked"
                          HorizontalAlignment="Stretch"
                          MinWidth="0"
                          MinHeight="0" />

                <controls:SvgIcon UriString="{StaticResource Infomaniak.Custom.Icons.drive-custom}"
                                  Foreground="{x:Bind Color, Converter={StaticResource ColorToBrushConverter}}"
                                  Height="16" />
                <TextBlock  Text="{x:Bind Name}"
                            Style="{StaticResource Infomaniak.Style.TextBlock.Body}"
                            VerticalAlignment="Center" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="MultiAccountDriveTemplate"
                      x:DataType="types:IDrive">
            <Grid ColumnSpacing="{StaticResource Infomaniak.Style.Spacing.XS}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Column="0"
                            Grid.Row="0"
                            Orientation="Horizontal">
                    <CheckBox Checked="DriveListCheckBox_Checked"
                              Unchecked="DriveListCheckBox_Unchecked"
                              HorizontalAlignment="Stretch"
                              MinWidth="0"
                              MinHeight="0" />

                    <controls:SvgIcon UriString="{StaticResource Infomaniak.Custom.Icons.drive-custom}"
                                      Foreground="{x:Bind Color, Converter={StaticResource ColorToBrushConverter}}"
                                      Height="16" />
                </StackPanel>
                <TextBlock Grid.Column="1"
                           Grid.Row="0"
                           Text="{x:Bind Name}"
                           Style="{StaticResource Infomaniak.Style.TextBlock.Body}"
                           VerticalAlignment="Center" />
                <TextBlock Grid.Column="1"
                           Grid.Row="1"
                           Text="{x:Bind AccountName}"
                           Style="{StaticResource Infomaniak.Style.TextBlock.Caption}"
                           Foreground="{ThemeResource TextFillColorSecondary}" />
            </Grid>
        </DataTemplate>
        <local:DriveTemplateSelector x:Key="DriveTemplateSelector"
                                     SingleAccountDriveTemplate="{StaticResource SingleAccountDriveTemplate}"
                                     MultiAccountDriveTemplate="{StaticResource MultiAccountDriveTemplate}" />
    </Page.Resources>

    <Grid HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Margin="{StaticResource Infomaniak.Style.Thickness.Uniform.XXL}"
          RowSpacing="{StaticResource Infomaniak.Style.Spacing.XL}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"
              RowSpacing="{StaticResource Infomaniak.Style.Spacing.L}">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0"
                        Spacing="{StaticResource Infomaniak.Style.Spacing.S}">
                <!-- Title -->
                <TextBlock x:Uid="Page_Onboarding_DriveSelectionPage_Title"
                           Style="{StaticResource Infomaniak.Style.TextBlock.Title}"
                           Foreground="{ThemeResource TextFillColorPrimary}" />

                <!-- User info -->
                <StackPanel Orientation="Horizontal"
                            Spacing="12"
                            VerticalAlignment="Center">
                    <PersonPicture ProfilePicture="{x:Bind ObViewModel.SelectedUser.AvatarImageSource, Mode=OneWay}"
                                   Height="24"
                                   Width="24" />
                    <TextBlock Text="{x:Bind ObViewModel.SelectedUser.Name}"
                               VerticalAlignment="Center"
                               Style="{StaticResource Infomaniak.Style.TextBlock.Body}"
                               Foreground="{ThemeResource TextFillColorSecondary}" />
                </StackPanel>
            </StackPanel>

            <!-- Drive available -->
            <Grid Grid.Row="1"
                  RowSpacing="{StaticResource Infomaniak.Style.Spacing.M}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Subtitle -->
                <TextBlock Grid.Row="0"
                           x:Uid="Page_Onboarding_DriveSelectionPage_Subtitle"
                           Style="{StaticResource Infomaniak.Style.TextBlock.BodySb}" />

                <!-- List of drives -->
                <ListView x:Name="DriveList"
                          Grid.Row="1"
                          SelectionMode="None"
                          IsItemClickEnabled="False"
                          ItemsSource="{x:Bind ObViewModel.SelectedUser.AllDrives, Mode=OneWay}"
                          HorizontalAlignment="Left">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding"
                                    Value="{StaticResource Infomaniak.Style.Thickness.Right.M}" />
                            <Setter Property="HorizontalContentAlignment"
                                    Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <!-- Data templates -->
                        <DataTemplate x:DataType="types:IDrive">
                            <Grid>
                                <ToolTipService.ToolTip>
                                    <ToolTip x:Uid="Page_Onboarding_DriveSelectionPage_DriveAlreadyConfigured"
                                             VerticalOffset="-80"
                                             IsEnabled="{x:Bind IsConfigured, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                                <Border Style="{StaticResource Infomaniak.Style.Border.WithContent}"
                                        Background="{ThemeResource LayerFillColorDefaultBrush}"
                                        MinHeight="44"
                                        MinWidth="280"
                                        Margin="{StaticResource Infomaniak.Style.Thickness.Bottom.XS}">
                                    <ContentControl Content="{x:Bind Mode=OneWay}"
                                                    ContentTemplateSelector="{StaticResource DriveTemplateSelector}"
                                                    VerticalContentAlignment="Center"
                                                    IsEnabled="{x:Bind IsConfigured, Converter={StaticResource BooleanToInvertedBooleanConverter}, Mode=OneWay}" />
                                </Border>

                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>

        <!-- Continue button -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Spacing="{StaticResource Infomaniak.Style.Spacing.XS}">

            <Button x:Name="AdvancedButton"
                    x:Uid="Page_Onboarding_DriveSelectionPage_AdvancedButton"
                    HorizontalAlignment="Left"
                    Style="{StaticResource SubtleButtonStyle}"
                    IsEnabled="{x:Bind ObViewModel.NewSyncs.Count, Converter={StaticResource IntegerToBooleanConverter}, Mode=OneWay}"
                    Click="AdvancedSettingsButton_Click" />

            <Button x:Uid="Page_Onboarding_DriveSelectionPage_FinishButton"
                    HorizontalAlignment="Left"
                    Style="{StaticResource AccentButtonStyle}"
                    IsEnabled="{x:Bind ObViewModel.NewSyncs.Count, Converter={StaticResource IntegerToBooleanConverter}, Mode=OneWay}"
                    Click="Finish_Click" />

        </StackPanel>

        <!-- Advanced settings dialog -->
        <ContentDialog x:Name="AdvancedSettingsDialog"
                       x:Uid="Page_Onboarding_DriveSelectionPage_AdvancedDialog"
                       IsPrimaryButtonEnabled="False"
                       CloseButtonClick="AdvancedSettingsDialog_CloseButtonClick"
                       Opened="AdvancedSettingsDialog_Opened">
            <ContentDialog.Title>
                <TextBlock x:Uid="Page_Onboarding_DriveSelectionPage_AdvancedDialog_Title"
                           Style="{StaticResource Infomaniak.Style.TextBlock.Subtitle}" />
            </ContentDialog.Title>
            <Grid RowSpacing="{StaticResource Infomaniak.Style.Spacing.S}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <InfoBar Grid.Row="0"
                         x:Uid="Page_Onboarding_DriveSelectionPage_AdvancedDialog_InfoBar"
                         IsOpen="True"
                         IsClosable="False"
                         Severity="Informational" />
                <ListView x:Name="DriveListView"
                          Grid.Row="1"
                          SelectionMode="None"
                          IsItemClickEnabled="False"
                          ItemsSource="{x:Bind ObViewModel.NewSyncs, Mode=OneWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:NewSync">
                            <Border Margin="{StaticResource Infomaniak.Style.Thickness.Uniform.XS}">
                                <Grid ColumnSpacing="{StaticResource Infomaniak.Style.Spacing.XS}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto" />
                                        <RowDefinition Height="auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>

                                    <controls:SvgIcon UriString="{StaticResource Infomaniak.Custom.Icons.drive-custom}"
                                                      Grid.Column="0"
                                                      Grid.Row="0"
                                                      Height="16"
                                                      VerticalAlignment="Center"
                                                      Foreground="{x:Bind Drive.Color, Converter={StaticResource ColorToBrushConverter}, Mode=OneWay}" />

                                    <TextBlock Grid.Column="1"
                                               Grid.Row="0"
                                               Text="{x:Bind Drive.Name, Mode=OneWay}"
                                               HorizontalAlignment="Left"
                                               VerticalAlignment="Center"
                                               Style="{StaticResource Infomaniak.Style.TextBlock.Body}" />

                                    <TextBlock Grid.Column="1"
                                               Grid.Row="1"
                                               Text="{x:Bind LocalPath, Mode=OneWay}"
                                               HorizontalAlignment="Left"
                                               VerticalAlignment="Top"
                                               Style="{StaticResource Infomaniak.Style.TextBlock.Body}"
                                               Foreground="{ThemeResource TextFillColorSecondary}"
                                               TextWrapping="Wrap" />

                                    <Button Grid.RowSpan="2"
                                            Grid.Column="2"
                                            x:Uid="Page_Onboarding_DriveSelectionPage_AdvancedDialog_CustomizeButton"
                                            Style="{StaticResource SubtleButtonStyle}"
                                            Click="ChangeSyncPathButton_Click" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TeachingTip x:Name="FolderSelectionError"
                             x:Uid="Page_Onboarding_DriveSelectionPage_AdvancedDialog_TeachingTip"
                             IsLightDismissEnabled="True"
                             PlacementMargin="20"
                             PreferredPlacement="Auto">
                </TeachingTip>
            </Grid>
        </ContentDialog>
    </Grid>
</Page>

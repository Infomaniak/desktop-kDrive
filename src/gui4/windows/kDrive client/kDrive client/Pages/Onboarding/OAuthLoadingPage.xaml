<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="Infomaniak.kDrive.Pages.Onboarding.OAuthLoadingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Infomaniak.kDrive"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:Infomaniak.kDrive.CustomControls"
      xmlns:converters="using:Infomaniak.kDrive.Converters"
      xmlns:viewmodels="using:Infomaniak.kDrive.ViewModels"
      mc:Ignorable="d">
    <Page.Resources>
        <converters:BooleanToInvertedVisibilityConverter  x:Key="BooleanToInvertedVisibilityConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanToInvertedBooleanConverter x:Key="BooleanToInvertedBooleanConverter" />
    </Page.Resources>

    <Grid>
        <StackPanel Orientation="Vertical"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="12">

            <!-- Title -->
            <TextBlock x:Uid="Page_Onboarding_OAuthLoadingPage_ConnectInNavigator_Title"
                       x:Name="TitleTextBlock"
                       Style="{StaticResource Infomaniak.Style.TextBlock.Title}"
                       Foreground="{ThemeResource TextFillColorPrimary}" />

            <!-- Subtitle -->
            <TextBlock x:Uid="Page_Onboarding_OAuthLoadingPage_ConnectInNavigator_Subtitle"
                       x:Name="SubtitleTextBlock"
                       Style="{StaticResource Infomaniak.Style.TextBlock.Body}"
                       TextWrapping="WrapWholeWords"
                       Foreground="{ThemeResource TextFillColorSecondary}" />

            <!-- Progress bar -->
            <ProgressBar x:Name="LoadingProgressBar"
                         Width="130"
                         IsIndeterminate="True"
                         Margin="0,8,0,0"
                         ShowPaused="False"
                         ShowError="False" />

            <!-- Restart oauth button -->
            <Button x:Name="RestartOAuthButton"
                    x:Uid="Page_Onboarding_OAuthLoadingPage_RestartOAuthButton"
                    Style="{StaticResource Infomaniak.Style.Button.Secondary}"
                    Click="RestartOAuthButton_Click"
                    Margin="0,24,0,0"
                    HorizontalAlignment="Center"
                    IsEnabled="False" />
        </StackPanel>
    </Grid>

</Page>

<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Infomaniak.kDrive.CustomControls.StatusBadge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Infomaniak.kDrive.CustomControls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="using:Infomaniak.kDrive.Converters"
             mc:Ignorable="d">

    <Border CornerRadius="{StaticResource Infomaniak.Style.CornerRadius.Circle}"
            BorderThickness="1"
            BorderBrush="{x:Bind Foreground, Mode=OneWay}">
        <Grid>
            <Ellipse Fill="{x:Bind Background, Mode=OneWay}" />
            <local:SvgIcon UriString="{x:Bind IconUri, Mode=OneWay}"
                           Foreground="{x:Bind Foreground, Mode=OneWay}"
                           Margin="{StaticResource Infomaniak.Style.Thickness.Uniform.XS}"/>
        </Grid>
    </Border>
</UserControl>

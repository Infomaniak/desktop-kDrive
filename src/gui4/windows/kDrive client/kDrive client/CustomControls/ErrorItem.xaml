<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="KDrive.CustomControls.ErrorItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:KDrive.CustomControls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="using:KDrive.ViewModels"
             xmlns:converters="using:KDrive.Converters"
             xmlns:viewmodels_error="using:KDrive.ViewModels.Errors"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanToInvertedVisibilityConverter  x:Key="BooleanToInvertedVisibilityConverter" />
        <converters:NodeTypeToSvgUriConverter x:Key="NodeTypeToSvgUriConverter" />
        <converters:StringPathToFileNameConverter x:Key="StringPathToFileNameConverter" />
        <converters:DateTimeToTimeAgoConverter x:Key="DateTimeToTimeAgoConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ImageIcon Grid.Column="0"
                       Grid.Row="0"
                       Height="16"
                       Width="16"
                       Margin="16,0,8,0">
                <ImageIcon.Source>
                    <SvgImageSource UriSource="{x:Bind Error.Icon, Mode=OneWay}" />
                </ImageIcon.Source>
            </ImageIcon>

            <TextBlock Grid.Column="1"
                       Grid.Row="0"
                       Text="{x:Bind Error.Title, Mode=OneWay}"
                       VerticalAlignment="Center"
                       Style="{StaticResource TextBlockStyle}"
                       Margin="0,0,0,4" />

            <TextBlock Grid.Column="1"
                       Grid.ColumnSpan="2"
                       Grid.Row="1"
                       Text="{x:Bind Error.HowToSolve, Mode=OneWay}"
                       VerticalAlignment="Center"
                       Style="{StaticResource CaptionSemiBoldTextBlockStyle}"
                       Margin="0,0,0,2"
                       Foreground="#666666" />

            <TextBlock Grid.Column="1"
                       Grid.ColumnSpan="2"
                       Grid.Row="2"
                       Text="{x:Bind Error.Cause, Mode=OneWay}"
                       TextWrapping="WrapWholeWords"
                       Margin="0,0,0,16"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="#666666" />
            <StackPanel Orientation="Horizontal"
                        Grid.Column="1"
                        Grid.Row="3">
                <Button Visibility="{x:Bind Error.HasSolveButton, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Content="{x:Bind Error.SolveButton.Text, Mode=OneWay}"
                        VerticalAlignment="Center"
                        Click="SolveButton_Click" />
                <HyperlinkButton Visibility="{x:Bind Error.HasInfoHyperLink, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                 Content="{x:Bind Error.InfoHyperLink.Text, Mode=OneWay}"
                                 VerticalAlignment="Center"
                                 Click="HyperlinkButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>

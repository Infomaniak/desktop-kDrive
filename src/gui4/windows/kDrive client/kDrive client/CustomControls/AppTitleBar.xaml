<?xml version="1.0" encoding="utf-8"?>
<TitleBar x:Class="Infomaniak.kDrive.CustomControls.AppTitleBar"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:local="using:Infomaniak.kDrive.CustomControls"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:controls="using:Infomaniak.kDrive.CustomControls"
          xmlns:converters="using:Infomaniak.kDrive.Converters"
          xmlns:kDrive="using:Infomaniak.kDrive"
          mc:Ignorable="d"
          Title="kDrive"
          Subtitle="Beta"
          HorizontalContentAlignment="Stretch"
          IsPaneToggleButtonVisible="False">

    <TitleBar.Resources>
        <converters:StringPathToFileNameConverter x:Key="StringPathToFileNameConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:IsNullToBoolOrVisibilityConverter x:Key="IsNullToBoolOrVisibilityConverter" />
    </TitleBar.Resources>
    <TitleBar.IconSource>
        <ImageIconSource>
            <ImageIconSource.ImageSource>
                <SvgImageSource UriSource="{StaticResource Infomaniak.Custom.Icons.kDrive}" />
            </ImageIconSource.ImageSource>
        </ImageIconSource>
    </TitleBar.IconSource>

    <TitleBar.Content>
        <Grid x:Name="TitleBarContentPanel"
              Height="32"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              ColumnSpacing="{StaticResource Infomaniak.Style.Spacing.S}"
              Visibility="{x:Bind TitleBarContentVisibility(ViewModel.SelectedSync, ViewModel.UpdateRequired), Mode=OneWay}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />

                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <controls:SyncStartPauseButton Grid.Column="0" />

            <!-- Middle: Search Box (centered in its column, which takes remaining space) -->
            <controls:SearchBox Grid.Column="1"
                                MinWidth="319"
                                Height="32" />
        </Grid>
    </TitleBar.Content>
</TitleBar>

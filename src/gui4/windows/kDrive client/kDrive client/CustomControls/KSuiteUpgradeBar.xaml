<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Infomaniak.kDrive.CustomControls.KSuiteUpgradeBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Infomaniak.kDrive.CustomControls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:Infomaniak.kDrive.CustomControls"
             mc:Ignorable="d">
    
    <Border CornerRadius="{StaticResource Infomaniak.Style.CornerRadius.M}"
            BorderThickness="1"
            Height="58">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0"
                                 EndPoint="1,0">
                <GradientStop Color="{ThemeResource kSuite.Gradient.lightblue}"
                              Offset="0.0" />
                <GradientStop Color="{ThemeResource kSuite.Gradient.darkblue}"
                              Offset="0.25" />
                <GradientStop Color="{ThemeResource kSuite.Gradient.purple}"
                              Offset="0.5" />
                <GradientStop Color="{ThemeResource kSuite.Gradient.pink}"
                              Offset="0.75" />
                <GradientStop Color="{ThemeResource kSuite.Gradient.orange}"
                              Offset="1.0" />
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Border.Background>
            <SolidColorBrush Color="{ThemeResource LayerFillColorDefault}" />
        </Border.Background>

        <Grid VerticalAlignment="Center"
              Padding="{StaticResource Infomaniak.Style.Thickness.Sides.Horizontal.M}"
              ColumnSpacing="{StaticResource Infomaniak.Style.Spacing.XS}">
            <!-- kSuite icon / message -->
            <StackPanel Orientation="Vertical"
                        HorizontalAlignment="Left"
                        Grid.Column="0"
                        Spacing="{StaticResource Infomaniak.Style.Spacing.XS}"
                        Padding="{StaticResource Infomaniak.Style.Thickness.Sides.Horizontal.M}">
                <controls:SvgIcon Height="16"
                                  HorizontalAlignment="Left"
                                  UriString="{ThemeResource Infomaniak.Custom.Icons.logo-myksuiteplus}" />
                <TextBlock x:Uid="CC_KSuiteUpgradeBar_message"
                           HorizontalAlignment="Left"
                           Style="{StaticResource Infomaniak.Style.TextBlock.Body}" />
            </StackPanel>

            <HyperlinkButton x:Name="UpgradeHyperLinkButton"
                             x:Uid="CC_KSuiteUpgradeBar_link"
                             HorizontalAlignment="Right"
                             HorizontalContentAlignment="Right"
                             Grid.Column="1"/>
        </Grid>
    </Border>
</UserControl>

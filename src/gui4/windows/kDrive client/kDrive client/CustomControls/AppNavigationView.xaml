<?xml version="1.0" encoding="utf-8"?>
<NavigationView x:Class="Infomaniak.kDrive.CustomControls.AppNavigationView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:local="using:Infomaniak.kDrive.CustomControls"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:controls="using:Infomaniak.kDrive.CustomControls"
                xmlns:converters="using:Infomaniak.kDrive.Converters"
                xmlns:loc="using:Infomaniak.kDrive.Localizer"
                mc:Ignorable="d"
                PaneDisplayMode="Left"
                SelectionChanged="OnSelectionChanged"
                ItemInvoked="OnItemInvoked"
                IsBackEnabled="{x:Bind ContentFrame.CanGoBack, Mode=OneWay}"
                BackRequested="OnBackRequested"
                OpenPaneLength="280"
                CompactPaneLength="52"
                Loaded="AppNavigationView_Loaded"
                Unloaded="AppNavigationView_Unloaded">
    <!--
    CompactPaneLength must be overridden (default is 48px) due to the following issue:
    https://github.com/microsoft/microsoft-ui-xaml/issues/10415

    Once the issue is resolved, we will be able to revert to the original value.
    -->

    <NavigationView.Resources>
        <converters:StringPathToFileNameConverter x:Key="StringPathToFileNameConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:IsNullToBoolOrVisibilityConverter x:Key="IsNullToBoolOrVisibilityConverter" />
    </NavigationView.Resources>

    <NavigationView.PaneCustomContent>
        <controls:SyncSelector HorizontalAlignment="Stretch"
                               Margin="4,9,4,20"
                               Visibility="{x:Bind IsPaneOpen, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}"
                               IsEnabled="{x:Bind ViewModel.SelectedSync, Converter={StaticResource IsNullToBoolOrVisibilityConverter}, ConverterParameter='Inverted=True', Mode=OneWay}" />
    </NavigationView.PaneCustomContent>

    <NavigationView.MenuItems>
        <NavigationViewItem Content="{x:Bind loc:Localizer.Instance.GetString('tabTitleHome')}"
                            Tag="HomePage"
                            MinHeight="30"
                            IsSelected="True"
                            IsEnabled="{x:Bind ViewModel.SelectedSync, Converter={StaticResource IsNullToBoolOrVisibilityConverter}, ConverterParameter='Inverted=True', Mode=OneWay}">
            <NavigationViewItem.Icon>
                <SymbolIcon Symbol="Home" />
            </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem Content="{x:Bind loc:Localizer.Instance.GetString('tabTitleActivities')}"
                            Tag="ActivityPage"
                            MinHeight="30"
                            IsEnabled="{x:Bind ViewModel.SelectedSync, Converter={StaticResource IsNullToBoolOrVisibilityConverter}, ConverterParameter='Inverted=True', Mode=OneWay}">
            <NavigationViewItem.Icon>
                <SymbolIcon Symbol="Sync" />
            </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem Content="{x:Bind loc:Localizer.Instance.GetString('tabTitleStorage')}"
                            Tag="StoragePage"
                            MinHeight="30"
                            IsEnabled="{x:Bind ViewModel.SelectedSync, Converter={StaticResource IsNullToBoolOrVisibilityConverter}, ConverterParameter='Inverted=True', Mode=OneWay}">
            <NavigationViewItem.Icon>
                <SymbolIcon Symbol="MapDrive" />
            </NavigationViewItem.Icon>
        </NavigationViewItem>
    </NavigationView.MenuItems>

    <NavigationView.PaneFooter>
        <NavigationViewItem Content="{x:Bind loc:Localizer.Instance.GetString('buttonOpenExplorer')}"
                            Tapped="NavigationViewItem_Tapped"
                            MinHeight="30"
                            IsEnabled="{x:Bind ViewModel.SelectedSync, Converter={StaticResource IsNullToBoolOrVisibilityConverter}, ConverterParameter='Inverted=True', Mode=OneWay}">
            <NavigationViewItem.Icon>
                <controls:SvgIcon Height="16"
                                  UriString="{StaticResource Infomaniak.DS.Icons.folder-circle-arrow-right}"
                                  Foreground="{ThemeResource TextFillColorPrimary}" />
            </NavigationViewItem.Icon>
        </NavigationViewItem>
    </NavigationView.PaneFooter>

    <Frame x:Name="ContentFrame" />
</NavigationView>

<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Infomaniak.kDrive.CustomControls.Errors.RemoteDiskFullBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Infomaniak.kDrive.CustomControls.Errors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="using:Infomaniak.kDrive.Converters"
             xmlns:controls="using:Infomaniak.kDrive.CustomControls"
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:IntegerToBooleanConverter x:Key="IntegerToBooleanConverter" />
    </UserControl.Resources>
    <InfoBar x:Uid="CC_RemoteDiskFullBar"
             Severity="Informational"
             IsOpen="{x:Bind ViewModel.SelectedSync.SyncErrors.Count, Converter={StaticResource IntegerToBooleanConverter}, Mode=OneWay}"
             IsClosable="True"
             Closed="InfoBar_Closed">
        <InfoBar.IconSource>
            <ImageIconSource>
                <ImageIconSource.ImageSource>
                    <SvgImageSource UriSource="{StaticResource Infomaniak.DS.Icons.Network.hardware}" />
                </ImageIconSource.ImageSource>
            </ImageIconSource>
        </InfoBar.IconSource>
        <InfoBar.ActionButton>
            <Button x:Uid="CC_RemoteDiskFullBar_Button"
                    HorizontalAlignment="Right"
                    Click="InfoBarButton_Click"
                    Style="{ThemeResource AccentButtonStyle}"
                    Visibility="{x:Bind Drive.IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" />
        </InfoBar.ActionButton>
    </InfoBar>
</UserControl>

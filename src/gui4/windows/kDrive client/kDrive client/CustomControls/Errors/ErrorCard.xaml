<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Infomaniak.kDrive.CustomControls.Errors.ErrorCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Infomaniak.kDrive.CustomControls.Errors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="using:Infomaniak.kDrive.Converters"
             xmlns:controls="using:Infomaniak.kDrive.CustomControls"
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:IsNullToBoolOrVisibilityConverter x:Key="IsNullToBoolOrVisibilityConverter" />
    </UserControl.Resources>
    <Grid Padding="{StaticResource Infomaniak.Style.Thickness.Uniform.XS}"
          Margin="{StaticResource Infomaniak.Style.Thickness.Sides.Vertical.XXS}"
          Background="{ThemeResource LayerFillColorDefault}"
          CornerRadius="{StaticResource Infomaniak.Style.CornerRadius.M}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Text="{x:Bind Title, Mode=OneWay}"
                   Style="{StaticResource BodyStrongTextBlockStyle}" />

        <!-- Item infos -->
        <StackPanel Grid.Row="1"
                    Grid.Column="0"
                    Orientation="Horizontal"
                    Spacing="{StaticResource Infomaniak.Style.Spacing.S}"
                    Visibility="{x:Bind ItemName, Converter={StaticResource IsNullToBoolOrVisibilityConverter}, ConverterParameter='Inverted=True'}">
            <controls:SvgIcon UriString="{x:Bind ItemIconUri, Mode=OneWay}"
                              Width="16"
                              Height="16" />

            <TextBlock Text="{x:Bind ItemName, Mode=OneWay}"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
        </StackPanel>

        <!-- Description -->
        <TextBlock Grid.Row="2"
                   Grid.Column="0"
                   Text="{x:Bind Description, Mode=OneWay}"
                   TextWrapping="Wrap"
                   Style="{StaticResource BodyTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

        <!-- Action button -->
        <Button Grid.Row="0"
                Grid.RowSpan="3"
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Margin="{StaticResource Infomaniak.Style.Thickness.Sides.Horizontal.M}"
                Content="{x:Bind ActionText}"
                Click="ActionButton_Click"
                Visibility="{x:Bind ActionText, Converter={StaticResource IsNullToBoolOrVisibilityConverter}, ConverterParameter='Inverted=True'}" />
    </Grid>
</UserControl>

<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Infomaniak.kDrive.CustomControls.Errors.ErrorCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Infomaniak.kDrive.CustomControls.Errors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="using:Infomaniak.kDrive.Converters"
             xmlns:controls="using:Infomaniak.kDrive.CustomControls"
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:IsNullToBoolOrVisibilityConverter x:Key="IsNullToBoolOrVisibilityConverter" />
        <converters:StringPathToFileNameConverter x:Key="StringPathToFileNameConverter" />
    </UserControl.Resources>
    <Grid Padding="{StaticResource Infomaniak.Style.Thickness.Uniform.S}"
          Margin="{StaticResource Infomaniak.Style.Thickness.Top.XS}"
          Background="{ThemeResource CardBackgroundFillColorDefault}"
          BorderThickness="1"
          BorderBrush="{ThemeResource CardStrokeColorDefault}"
          CornerRadius="{StaticResource Infomaniak.Style.CornerRadius.Uniform.S}"
          RowSpacing="{StaticResource Infomaniak.Style.Spacing.XS}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Text="{x:Bind Title, Mode=OneWay}"
                   Style="{StaticResource BodyTextBlockStyle}" />

        <!-- Item infos -->
        <Border Grid.Row="1"
                Grid.Column="0"
                Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                Padding="{StaticResource Infomaniak.Style.Thickness.Sides.Horizontal.XS}"
                Visibility="{x:Bind ItemPath, Converter={StaticResource IsNullToBoolOrVisibilityConverter}, ConverterParameter='Inverted=True'}"
                CornerRadius="{StaticResource Infomaniak.Style.CornerRadius.Uniform.XS}"
                HorizontalAlignment="Left"
                VerticalAlignment="Top">
            <Grid ColumnSpacing="{StaticResource Infomaniak.Style.Spacing.XS}"
                  Padding="{StaticResource Infomaniak.Style.Thickness.Sides.Vertical.XXS}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <ToolTipService.ToolTip>
                    <ToolTip Content="{x:Bind ItemPath, Mode=OneWay}"
                             Placement="Right" />
                </ToolTipService.ToolTip>
                <controls:SvgIcon Grid.Column="0"
                                  UriString="{x:Bind ItemIconUri, Mode=OneWay}"
                                  Width="12"
                                  Height="12" />

                <TextBlock Grid.Column="1"
                           Margin="0"
                           Padding="0"
                           Text="{x:Bind ItemPath, Converter={StaticResource StringPathToFileNameConverter}, Mode=OneWay}"
                           Style="{StaticResource BodyTextBlockStyle}" />
            </Grid>
        </Border>

        <!-- Description & CustomContent-->
        <StackPanel Grid.Row="2"
                    Grid.Column="0">
            <TextBlock Text="{x:Bind Description, Mode=OneWay}"
                       TextWrapping="Wrap"
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <ContentPresenter Content="{x:Bind CustomContent, Mode=OneWay}" />
        </StackPanel>

        <!-- Action button -->
        <Button Grid.Row="0"
                Grid.RowSpan="3"
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Margin="{StaticResource Infomaniak.Style.Thickness.Sides.Horizontal.M}"
                Content="{x:Bind ActionText}"
                Click="ActionButton_Click"
                Visibility="{x:Bind ActionText, Converter={StaticResource IsNullToBoolOrVisibilityConverter}, ConverterParameter='Inverted=True'}" />
    </Grid>
</UserControl>

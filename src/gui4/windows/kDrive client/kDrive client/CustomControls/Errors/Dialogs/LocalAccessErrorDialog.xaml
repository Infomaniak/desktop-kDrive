<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="Infomaniak.kDrive.CustomControls.Errors.LocalAccessErrorDialog"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Infomaniak.kDrive.CustomControls.Errors"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:Infomaniak.kDrive.CustomControls"
      xmlns:kDrive="using:Infomaniak.kDrive"
      mc:Ignorable="d">

    <StackPanel Spacing="{StaticResource Infomaniak.Style.Spacing.S}">
        <StackPanel Spacing="{StaticResource Infomaniak.Style.Spacing.XS}">
            <TextBlock TextWrapping="Wrap"
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}">
            <Run Text="{x:Bind kDrive:Localizer.Instance.GetString('localFileAccessErrorDialogDescription')}" />
            <Hyperlink NavigateUri="https://support.infomaniak.com/"
                       UnderlineStyle="None">
                <Run Text="{x:Bind kDrive:Localizer.Instance.GetString('localFileAccessErrorDialogFaqLink')}" />
            </Hyperlink>
            </TextBlock>

            <controls:ItemNamePresenter ItemPath="{x:Bind FilePath}"
                                        NodeType="{x:Bind _error.NodeType}" />
        </StackPanel>
        <StackPanel Spacing="{StaticResource Infomaniak.Style.Spacing.XS}">
            <TextBlock Text="{x:Bind kDrive:Localizer.Instance.GetString('localFileAccessErrorDialogLocationLabel')}" />

            <controls:ItemPathPresenter RelativeItemPath="{x:Bind FilePath}"
                                        HorizontalAlignment="Stretch"
                                        Sync="{x:Bind Sync}" />
        </StackPanel>
    </StackPanel>
</Page>

<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Infomaniak.kDrive.CustomControls.Errors.Templates.Node.QuotaExceededError"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Infomaniak.kDrive.CustomControls.Errors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="using:Infomaniak.kDrive.Converters"
             xmlns:kDrive="using:Infomaniak.kDrive"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <local:ErrorCard Visibility="{x:Bind Error.Sync.Drive.IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}"
                         Title="{x:Bind kDrive:Localizer.Instance.GetString('errQuotaExceededTitle')}"
                         Description="{x:Bind kDrive:Localizer.Instance.GetStringCombine1s('errQuotaExceededDescription', Error.NodeTypeTranslationKey)}"
                         ActionText="{x:Bind kDrive:Localizer.Instance.GetString('buttonManageStorage')}"
                         ActionClick="ErrorCard_ActionClick"
                         Error="{x:Bind Error}" />

        <local:ErrorCard Visibility="{x:Bind Error.Sync.Drive.IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter='Inverted=True', Mode=OneWay}"
                         Title="{x:Bind kDrive:Localizer.Instance.GetString('errQuotaExceededTitle')}"
                         Description="{x:Bind kDrive:Localizer.Instance.GetStringCombine1s('errQuotaExceededDescription', Error.NodeTypeTranslationKey)}"
                         Error="{x:Bind Error}" />
    </Grid>
</UserControl>
<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Infomaniak.kDrive.CustomControls.Errors.Templates.Node.MoveCancelError"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Infomaniak.kDrive.CustomControls.Errors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="using:Infomaniak.kDrive.Converters"
             xmlns:controls="using:Infomaniak.kDrive.CustomControls"
             xmlns:kDrive="using:Infomaniak.kDrive"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:FilePathToIconConverter x:Key="FilePathToIconConverter" />
    </UserControl.Resources>
    <local:ErrorCard x:Name="ErrorCard"
                     Title="{x:Bind kDrive:Localizer.Instance.GetString('errForbiddenActionTitle')}"
                     Description="{x:Bind kDrive:Localizer.Instance.GetStringCombine1s('errMoveCancelDescription', Error.NodeTypeTranslationKey)}"
                     Error="{x:Bind Error}">
        <local:ErrorCard.CustomContent>
            <controls:ItemPathPresenter RelativeItemPath="{x:Bind Error.DestinationPath}"
                                        Sync="{x:Bind Error.Sync}"
                                        Margin="{StaticResource Infomaniak.Style.Thickness.Top.S}"/>
        </local:ErrorCard.CustomContent>
    </local:ErrorCard>
</UserControl>
<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Infomaniak.kDrive.CustomControls.UpdateRequiredControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Infomaniak.kDrive.CustomControls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="using:Infomaniak.kDrive.Converters"
             xmlns:kDrive="using:Infomaniak.kDrive"
             Unloaded="UserControl_Unloaded"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:IsNullToBoolOrVisibilityConverter x:Key="IsNullToBoolOrVisibilityConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            Margin="{StaticResource Infomaniak.Style.Thickness.FullScreenCard}"
            CornerRadius="{StaticResource Infomaniak.Style.CornerRadius.Uniform.M}">
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center">

            <local:SvgIcon Height="134"
                           UriString="{ThemeResource Infomaniak.Custom.Images.kDrive-folder-sync-warning}"
                           Margin="{StaticResource Infomaniak.Style.Thickness.Bottom.XL}" />

            <TextBlock  HorizontalAlignment="Center"
                        Style="{ThemeResource SubtitleTextBlockStyle}"
                        Text="{x:Bind kDrive:Localizer.Instance.GetString('updateRequiredTitle')}"
                        Margin="{StaticResource Infomaniak.Style.Thickness.Bottom.M}" />

            <TextBlock  HorizontalAlignment="Center"
                        Text="{x:Bind kDrive:Localizer.Instance.GetString('updateRequiredDescription')}"
                        TextAlignment="Center"
                        Margin="{StaticResource Infomaniak.Style.Thickness.Bottom.XL}" />

            <Button HorizontalAlignment="Center"
                    Style="{ThemeResource AccentButtonStyle}"
                    Visibility="{x:Bind UpdateButtonVisibility(ViewModel.Settings.UpdateManager.FetchingUpdate, ViewModel.Settings.UpdateManager.AvailableUpdate), Mode=OneWay}"
                    IsEnabled="{x:Bind ViewModel.Settings.UpdateManager.AvailableUpdate, Converter={StaticResource IsNullToBoolOrVisibilityConverter}, ConverterParameter='Inverted=True', Mode=OneWay}"
                    Click="UpdateButton_Click">
                <Grid>
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Visibility="{x:Bind ViewModel.Settings.UpdateManager.FetchingUpdate, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}">
                        <TextBlock Text="{x:Bind kDrive:Localizer.Instance.GetString('buttonDownloadInProgress')}"
                                   VerticalAlignment="Center"
                                   Margin="{StaticResource Infomaniak.Style.Thickness.Right.M}" />
                        <ProgressRing Height="16"
                                      Width="16"
                                      IsIndeterminate="True"
                                      Foreground="{ThemeResource TextOnAccentFillColorDefaultBrush}" />
                    </StackPanel>
                    <TextBlock Text="{x:Bind kDrive:Localizer.Instance.GetString('buttonUpdateNow')}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Visibility="{x:Bind ViewModel.Settings.UpdateManager.FetchingUpdate, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter='Inverted=True', Mode=OneWay}" />
                </Grid>
            </Button>

            <Button HorizontalAlignment="Center"
                    Style="{ThemeResource AccentButtonStyle}"
                    Visibility="{x:Bind ManualUpdateButtonVisibility(ViewModel.Settings.UpdateManager.FetchingUpdate, ViewModel.Settings.UpdateManager.AvailableUpdate), Mode=OneWay}"
                    Click="ManualUpdateButton_Click"
                    Content="{x:Bind kDrive:Localizer.Instance.GetString('buttonDownloadManually')}" />

        </StackPanel>
    </Border>
</UserControl>

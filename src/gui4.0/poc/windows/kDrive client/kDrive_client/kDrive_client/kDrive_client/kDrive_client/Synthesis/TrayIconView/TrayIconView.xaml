<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="kDrive_client.Synthesis.TrayIconView.TrayIconView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:kDrive_client.Synthesis.TrayIconView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="using:H.NotifyIcon"
    mc:Ignorable="d">

    <tb:TaskbarIcon
        x:FieldModifier="public"
        ContextMenuMode="SecondWindow"
        LeftClickCommand="{x:Bind ShowHideWindowCommand}"
        NoLeftClickDelay="True"
        ToolTipText="ToolTip"
        >
        <tb:TaskbarIcon.IconSource>
        <tb:GeneratedIconSource
            Text="K"
            Foreground="SkyBlue"
            />
    </tb:TaskbarIcon.IconSource>
        <tb:TaskbarIcon.ContextFlyout>
            <MenuFlyout AreOpenCloseAnimationsEnabled="False">
                <MenuFlyoutItem
                    Command="{x:Bind ShowHideWindowCommand}"
                    Text="Show/Hide Window"
                    />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem
                    Command="{x:Bind ExitApplicationCommand}"
                    Text="Exit"
                    />
            </MenuFlyout>
        </tb:TaskbarIcon.ContextFlyout>
    </tb:TaskbarIcon>
</UserControl>

<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="kDrive_client.DrivePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:kDrive_client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="FolderTemplate" x:DataType="local:ExplorerItem">
            <TreeViewItem AutomationProperties.Name="{x:Bind Name}" 
    ItemsSource="{x:Bind Children}">
                <RelativePanel>
                    <StackPanel Orientation="Horizontal">
                        <Image Width="20" Source="/Assets/resources/icons/document types/folder.svg"/>
                        <TextBlock Margin="0,0,10,0"/>
                        <TextBlock Text="{x:Bind Name}" />
                    </StackPanel>
                    <ProgressRing IsActive="True" Height="10" Width="10" RelativePanel.AlignRightWithPanel="True" Visibility="{Binding SizeLoadingVisibility, Mode=OneWay}" />
                    <TextBlock Text="{Binding Size, Mode=OneWay}" RelativePanel.AlignRightWithPanel="True" Visibility="{Binding SizeVisibility, Mode=OneWay}" />
                </RelativePanel>
            </TreeViewItem>
        </DataTemplate>

        <DataTemplate x:Key="FileTemplate" x:DataType="local:ExplorerItem">
            <TreeViewItem AutomationProperties.Name="{x:Bind Name}">
                <StackPanel Orientation="Horizontal">
                    <Image Width="20" Source="/Assets/resources/icons/document types/file-default.svg"/>
                    <TextBlock Margin="0,0,10,0"/>
                    <TextBlock Text="{x:Bind Name}" />
                </StackPanel>
            </TreeViewItem>
        </DataTemplate>

        <DataTemplate x:Key="LoadingPlaceHolderTemplate" x:DataType="local:ExplorerItem">
            <TreeViewItem AutomationProperties.Name="{x:Bind Name}" ScrollViewer.VerticalScrollMode="Enabled">
                <StackPanel Orientation="Horizontal">
                    <Image Width="20" Source="/Assets/resources/icons/document types/folder.svg" />
                    <ProgressBar Width="130" IsIndeterminate="True" ShowPaused="False" ShowError="False" Margin="15,0,0,0"/>
                </StackPanel>
            </TreeViewItem>
        </DataTemplate>
        <local:ExplorerItemTemplateSelector x:Key="ExplorerItemTemplateSelector" FolderTemplate="{StaticResource FolderTemplate}" FileTemplate="{StaticResource FileTemplate}" LoadingPlaceHolder="{StaticResource LoadingPlaceHolderTemplate}"   />
    </Page.Resources>

    <Grid>
        <TreeView x:Name="FolderTree" ItemsSource="{x:Bind DataSource}" ScrollViewer.VerticalScrollMode="Enabled"
                   ItemTemplateSelector="{StaticResource ExplorerItemTemplateSelector}" SelectionMode="Multiple" Expanding="FolderTree_Expanding"/>


    </Grid>
</Page>
